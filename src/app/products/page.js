import Link from 'next/link'
import React from 'react'

const Products = ({ searchParams }) => {
    // console.log("234", searchParams.search)
    return (
        <div>
            <h3 className='grid grid-rows-[20px_1fr_20px] items-center justify-items-center min-h-screen p-8 pb-20 gap-16 sm:p-20 font-[family-name:var(--font-geist-sans)]'> Products Page </h3>

        </div>
    )
}

export default Products
